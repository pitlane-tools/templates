# Required outside the default local development command (`vp dev` starts a
# project-local PGlite database and injects DATABASE_URL automatically).
# In production, Netlify DB injects NETLIFY_DATABASE_URL for you — provision it
# once with `vpx netlify-cli db init`. Set DATABASE_URL only when pointing at
# another PostgreSQL server.
DATABASE_URL=
