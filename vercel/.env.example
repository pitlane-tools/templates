# Required outside the default local development command (`vp dev` starts a
# project-local PGlite database and injects DATABASE_URL automatically).
# In production, set this to the connection string for your hosted PostgreSQL
# database — on Vercel, provision one from the Marketplace (e.g. Neon) and it
# lands in your project's environment variables.
DATABASE_URL=
